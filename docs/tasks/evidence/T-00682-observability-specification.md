# T-00682 — Repository Health / observability: Specification
## Contract: Each `RepoHealthCheck` carries `duration_ms`. `RepoHealthReport` carries aggregate counters and `timestamp_utc`. CLI `--json` and MCP emit full structured reports.
