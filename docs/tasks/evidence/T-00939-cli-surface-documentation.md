# T-00939 — Agent Handoff Protocol / CLI Surface: Documentation

## 1. Documentation Scope
- Updated `docs/README.md` with:
  - CLI subcommand reference (`list`, `show`, `initiate`, `accept`, `reject`, `complete`, `cancel`).
  - Standalone test runner instructions (`tools/test_handoff_suites.py`).
  - Criterion `H3` test output and evidence links.
- Updated `progress.md` with milestone records for `T-00931..T-00940`.
- Verified documentation invariants C1..C6 via `tools/check_task_docs.py`.
