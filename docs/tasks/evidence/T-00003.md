# T-00003 — Generate canonical 10,000-task ledger

**Completed:** 2026-08-21

## Acceptance criteria
- [x] Ledger has 10,000 well-formed tasks; pointer file exists.

## What exists

- `docs/tasks/MASTER_TASK_LEDGER.jsonl` — 10,000 tasks, IDs T-00001..
  T-10000, each with `instructions`, `acceptance`, `depends_on: [id-1]`,
  and `next_task: id+1` (last task has `next_task: null`).
- `docs/tasks/TASK_STATE.json` — live pointer file (schema v2).

## Verification (live)

```
$ python3 tools/task_ledger.py check
{ "ok": true, "total_tasks": 10000 }
```

Invariant checks pass: sequential IDs, no gaps, `depends_on` chains
strictly linear, `next_task` pointers correct. Pointer file exists and
advances one step per completion (reconciled to `next_task: 16` after
T-00004..T-00015; see TASK_STATE.json).
