# T-00676 — Repository Health / security policy: Integration
## Verified: `check_security_governance` is called by the `check_repo_health` orchestrator.
## CLI `aiosh repo health` includes it. MCP `aios.repo.health` includes it. All suites PASS.
