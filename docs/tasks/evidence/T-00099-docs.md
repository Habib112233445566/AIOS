# T-00099 — documentation component: Documentation (evidence)

**Date:** 2026-08-22 · **Artifact:** `docs/README.md` — new section
"Documentation invariants (Task Ledger Control, T-00091..T-00100)".

## Contents of the doc update

1. **What shipped + how to invoke:** full copy-pasteable run of
   `python3 tools/check_task_docs.py` with its real six-line output
   shape (verified against the live tree before pasting), plus the CI
   suite names it now runs under (`task_docs_unit`,
   `task_docs_scaffold`).
2. **Per-check semantics:** one line each for C1..C6 as the operator
   will see them.
3. **Limitations stated, not omitted (L-D1..L-D5):** structural checks
   only (not prose quality); frozen C2 list grows monotonically with
   newly closed components; C5 boundary = repo root; 16 MiB read cap;
   deliberately not agent-reachable over MCP.
4. **Evidence links:** T-00091 → T-00098 chain + T-00100 placeholder,
   all under `docs/tasks/evidence/`.

## Acceptance check

- [x] README/spec updated with what shipped and how to invoke
- [x] ≥1 copy-pasteable example command (with expected output)
- [x] Constraints/limitations recorded explicitly
- [x] Task evidence files linked from the doc

Post-edit verification: `check_task_docs.py` still C1–C6 PASS on the
live tree (the new section itself obeys C5/C6 — links in-tree, no
volatile counts).

## CORRECTION (process slip, fixed same session)

The first post-edit verification DID fail (C5: the new README section
quoted the checker's example output containing the literal marker word
— a self-referential trip of the very rule it documents), but the
completion guard piped through `| tail -1`, which masked exit 1, so
T-00099 got marked done while the tree was red. Found and fixed within
minutes: example-output lines reworded marker-free (same precedent as
SPEC §8.5 "marker-word reintroduction"); tree re-verified C1–C6 PASS,
exit 0. Lesson encoded for T-00100 verification: completion guards must
test PIPESTATUS, never tail's exit code. This addendum is part of the
permanent record.
