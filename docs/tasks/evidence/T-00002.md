# T-00002 — Environment + baseline verification

**Completed:** 2026-08-21

## Acceptance criteria
- [x] All baseline suites PASS; evidence captured.

## Environment

- Python deps: `pip install -e code/aiosh-mcp` (the legacy TS CLI's
  `aiosh run` shells out to `python3 -m aiosh_mcp.sandbox`, so the
  editable install is a hard requirement for the legacy suites).
- Node deps: `code/aiosh-cli/node_modules` + `dist/` built via `npx tsc`.
- Rust toolchain (rewrite): `rustup`/`cargo` 1.98, workspace at
  `code/aiosh-rust/`.

## Baseline run (live, 2026-08-21)

`bash ci/run_all_smokes.sh` — all 10 suites PASS, exit 0:

```
rust_smoke, classifier_smoke, mcp_smoke, pentest_smoke,
sandbox_smoke, retention_smoke, demo_smoke, cli_bash_smoke,
task_ledger_unit, task_ledger_scaffold
```

(Full pass verified again after the T-00004..T-00015 reconciliation —
see T-00004 evidence for the suite table.)

## Note

The very first CI attempt in this environment FAILED on `mcp_smoke`
(`ModuleNotFoundError: aiosh_mcp`) precisely because the editable install
was absent — which is what T-00002 was meant to establish. Installing it
fixed the suite; the full pass is captured in `ci/run_all_smokes.sh`
output and T-00004 evidence.
