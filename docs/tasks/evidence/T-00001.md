# T-00001 — Mission lock + sequential execution law

**Completed:** 2026-08-21

## Acceptance criteria
- [x] `docs/tasks/GOALS.md` exists and states the no-skip rule.

## What was done

Read the governing docs (`START_HERE.md`, `PROJECT_MANIFEST.yaml`,
`AI_CONSTITUTION.md`, ADR-0035, ADR-0036) and created
`docs/tasks/GOALS.md` with:

- the mission (AI-native OS: Pillars A/B/C + S-rank AI subsystem),
- the pillars, and
- the **sequential execution law** — tasks execute strictly in numeric
  order; task N+1 starts only after N is done; agents read
  `docs/tasks/TASK_STATE.json` and execute ONLY `next_task`.

## Verification

`docs/tasks/GOALS.md` exists (3.4 KB) and states the no-skip rule; the
rule is mechanically enforced by `tools/complete_task.py` (T-00005) and
mirrored in `README.md` (T-00006).
