# T-00006 — Index task system from README

**Completed:** 2026-08-21

## Acceptance criteria
- [x] README links the ledger and states the no-skip rule.

## What was done

- `README.md` — new **"Task ledger (read this before doing any work)"**
  section:
  - Points to `docs/tasks/MASTER_TASK_LEDGER.jsonl` (T-00001..T-10000,
    human-readable index in `docs/tasks/MASTER_TASK_LEDGER.md`) and the
    live pointer `docs/tasks/TASK_STATE.json`.
  - States the rule verbatim: *"agents must read
    `docs/tasks/TASK_STATE.json` and execute ONLY `next_task`"*; run
    `python3 tools/complete_task.py <id>`; never jump ahead; run
    `bash ci/run_all_smokes.sh` before and after any code change.
- `docs/README.md` — task-ledger section linking the same files and the
  no-skip rule.
- Repo tree listing in `README.md` updated to include `docs/tasks/`.

## Verification

Both files link the ledger and state the no-skip rule; the rule is
also echoed in `docs/tasks/GOALS.md` (T-00001) and enforced mechanically
by `tools/complete_task.py` (T-00005).
