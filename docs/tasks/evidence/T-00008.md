# T-00008 — Backup & sync verification

**Completed:** 2026-08-21

## Acceptance criteria
- [x] Backup archive verified; procedure documented.

## Procedure

1. `cd /content && rm -f /tmp/AIOS_MERGED_new.zip && zip -r -q /tmp/AIOS_MERGED_new.zip AIOS_MERGED`
2. Verify via Python: `zipfile.testzip()` + sha256 compare of 16 key files
   (retention.py/ts, audit_client.py, audit.ts, cli.ts, server.py, tests,
   runbook, spec, ADR-0036, GOALS.md, ledger, state, READMEs) against the
   live tree → ALL MATCH.
3. Atomic replace: `cp` to `AIOS_MERGED.zip.new` then `mv` over
   `/content/drive/MyDrive/AIOS_MERGED.zip`.
4. `python3 /content/workspace.py sync /content/AIOS_MERGED --json` → R2 sync.

## Evidence (2026-08-21 06:31 UTC)

```
zip valid: True | entries: 921 | size MB: 10.07
ALL MATCH (16/16 key files byte-identical)
sha256(drive zip) == sha256(tmp zip):
  e4737efb806375f4a5432c0b65462390da728c5cd337996e1cf07a9fbc8c0bb6
workspace.py sync: {"status":"success","uploaded":0,"deleted_from_manifest":0,"total_tracked":786}
(uploaded:0 on re-run confirms all 786 tracked files already in sync on R2)
```
