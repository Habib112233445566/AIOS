# T-00949 — Agent Handoff Protocol / MCP/API Surface: Documentation

## 1. Documentation Scope
- Updated `docs/README.md` with:
  - MCP tool endpoints reference (`aios.handoff.list`, `show`, `initiate`, `accept`, `reject`, `complete`, `cancel`).
  - Standalone test runner instructions (`tools/test_handoff_suites.py`).
  - Criterion `H4` test output and evidence links.
- Updated `progress.md` with milestone records for `T-00941..T-00950`.
- Verified documentation invariants C1..C6 via `tools/check_task_docs.py`.
