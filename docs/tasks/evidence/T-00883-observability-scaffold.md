# T-00883 — Regression Triage / Observability: Scaffold

## 1. Scaffold Deliverables
- Added method signatures `status_counts()`, `severity_counts()`, and `summary_line()` to `TriageReport` in `aiosh-core::triage`.
- Verified clean compilation across `aiosh-core`, `aiosh-mcp`, `aiosh-sandbox`, and `aiosh-cli`.
