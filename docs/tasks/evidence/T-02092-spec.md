# Specification Summary: T-02092 (recovery & validation: Specification)

- **Sub-Epic**: Sub-Epic 10: Capability Model Recovery & Validation Subsystem
- **Invariants**: `CAPREC1..CAPREC6`
- **Core Types**:
  - `CapabilityRecoveryAction` (`LoadedExisting`, `CreatedDefaultFresh`, `RecoveredFromBackup`)
  - `CapabilityValidationReport` (`valid_capabilities`, `invalid_capabilities`, `total_capabilities`, `errors`, `warnings`, `healthy`)
- **Key Guarantees**:
  - Partition consistency: valid + invalid == total
  - Quarantine backup before store recreation
  - Lineage resolution and monotonic attenuation checks
