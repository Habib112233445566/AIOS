# T-00089 — observability: Documentation (evidence)

**Date:** 2026-08-22
**Artifact:** `docs/SPEC-TASK-LEDGER.md` updated.

## What shipped in the doc

1. **§8 action table** — `metrics` added to the read-only grant-free row.
2. **New §8.6 "Observability — the `metrics` action (T-00081..T-00090)"**
   covering:
   - the stable additive-only key set `{tasks, audit, config}` and exactly
     which fields each carries (counts only; no titles/ids/notes);
   - grant policy + one-honest-row semantics incl. the documented
     Python pre-gate vs Rust audited-refusal nuance;
   - TWO copy-pasteable examples (CLI + MCP wire), both verified live
     earlier this session (T-00085/86 probes);
   - limitations L-O1..L-O3 stated explicitly (verify cost O(live rows),
     parity nuance, eventually-consistent read) — not omitted;
   - links to all component evidence files T-00082..T-00090.

## Acceptance check

- [x] README/spec updated with what shipped + how to invoke (§8.6)
- [x] ≥1 copy-pasteable example (two provided, live-verified)
- [x] Constraints/limitations recorded (L-O1..L-O3)
- [x] Task evidence files linked from the doc
